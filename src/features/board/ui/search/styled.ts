import styled from "@emotion/styled";
import { colors } from "@/shared/lib";

export const SearchInput = styled.input`
  background: #fff;
  border: none;
  outline: none;
  border-bottom: 1px solid ${colors.black.default};
`;
